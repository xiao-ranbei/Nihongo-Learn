# Nihongo-Learn · UI 系统性审查报告

> 审查人：UI Designer　日期：2026-08-25　范围：index.html / verb-conjugation-stamp.html / adj-noun-stamp.html / particles-stamp.html / counter-stamp.html
> 方法：WCAG 2.1 AA 对比度实测 + 代码级样式审计（字号/间距/状态/断点/无障碍）
> 结论：整体设计语言统一、信息架构清晰，属**完成度较高的自洽系统**；主要短板集中在**可读性对比度、键盘可达性、字号约束、间距节奏**四方面。
>
> **修复进度（2026-08-25）**：
> - ✅ P0 对比度 + 全局焦点（commit `42e089d`）
> - ✅ P1 字阶收敛（commit `42e089d`）
> - ✅ P2 间距网格 / 触控目标 / 中间断点（commit `ef2f153`）
> - ✅ P3 锚点导航 / 练习设置折叠 / ESC（commit `ef2f153`）
> - ✅ 风格统一：顶部框体与 note 外壳 → 双层线条 + 直角（commit `42e089d`）
>
> **追加优化（2026-08-27）**：规则表「活用形」首列改为固定宽度的层级化标签牌，内容垂直居中，加入序号、主次字号和读音行，降低复杂背景下的文字堆叠感。
> 五段音变速查同步改为表内分组口诀行：促音便、い音便、し音便、拨音便各自直接显示词尾范围与 `て／た` 结果，`行く` 例外置于表尾，移除表外长段速记。
> 派生形分类筛选从标签栏内部移至标签栏下方的独立筛选栏；规则表「活用形」首列收窄为桌面 132px、移动端 112px，释放横向空间。
> 全站风格统一：首页与两个学习页采用同一纸面纹理、键盘焦点、音变速查表表头/行悬停和间距语言；形容词/名词页的速查区外框与动词页保持同源。
> 五段音变表的结果单元格增加“代表词例 + 振假名 + 变形结果”，并统一表头、居中对齐和移动端横向滚动体验；形容词/名词速查表补齐“词类”表头以保持列对齐。
>
> **规则图谱记忆层（2026-08-27）**：动词页新增五段音段变化轴、`書く` 常用变形路径、派生形用途分组和例外记忆卡；形容词/名词页新增两条核心公式、三类词六种谓语路径、定语/副词/名词连接分层和易错词卡。两页均将完整规则表保留为可展开查询层，避免默认入口被密集表格占据。
>
> **类型详情修正（2026-08-27）**：动词页将四类动词改为可切换标签；每个标签都展示对应的六种常用变化和派生形结果。标签统一标注为“一类･五段动词、二类･一段动词、三类･カ变动词、三类･サ变动词”，避免视觉上只剩五段动词。

---

## 〇、审查亮点（保持现状，勿回退）

| 维度 | 现状 |
|---|---|
| 设计语言 | 印谱方格视觉体系完整统一，三页同源；主题系统 5 套可切换且全站持久化 |
| 表格可用性 | 规则表 `thead th` 纵向 sticky + `.form-cell` 首列横向 sticky，长表滚动体验良好 |
| 无障碍基础 | 语义化标签、`fieldset/legend` 分组、`aria-live` 播报、`prefers-reduced-motion` 均已具备 |
| 空状态 | 练习无会话时给出引导提示；错题为空时禁用按钮 |
| 性能 | 零依赖、无框架、数据外置、CSS 变量统一管理 |

---

## 一、配色与对比度（WCAG AA）— 问题最集中

> 实测 5 套主题 × 常用前景/背景组合，**11 项不达标**。次要文字是重灾区。

| # | 严重度 | 组合 | 实测 | 要求 | 位置 |
|---|---|---|---|---|---|
| C1 | 🔴 高 | `--muted` on `--paper`（#806e68/#f6f3ee） | **4.36:1** | 4.5 | 全站说明文字：面板 intro、卡片 note、练习说明 |
| C2 | 🔴 高 | `--muted` on `--wash`（#806e68/#f0ece6） | **4.10:1** | 4.5 | intro-note、速记、工具条说明 |
| C3 | 🔴 高 | `--accent-muted` on `--surface`（#9b8978/#fffdfa） | **3.31:1** | 4.5 | 首页卡片 footer、页脚、卡片 footer 提示 |
| C4 | 🟠 中 | `--success` on `--success-soft`（#4b8068/#e4f0e7） | **3.91:1** | 4.5 | "现在可学/正确"徽标小字 |
| C5 | 🟠 中 | `--danger` on `--danger-soft`（#a75550/#f6e3e0） | **4.18:1** | 4.5 | 练习错误提示 |
| C6 | 🟡 低 | 白字 on `--vermilion`（#ffffff/#b9655b） | 4.14:1 | 大字号 3.0 ✓ | 印章大字通过；若用于小字需注意 |

**主题扩展性**：墨流/苔庭/蓝染/藤棚 四套的 `--muted` on paper 实测 3.86–4.22:1，**全部不达标**——不是单主题问题，是 muted 基准色整体偏浅。

**修复建议**：将 `--muted` 加深一档（如默认主题 #6b5b56，并让各主题 muted 对齐 ≥4.6:1）；`--accent-muted` 加深至 #8a7766 左右；success/danger 各加深一档。改 themes.css 一处即可全站生效（这正是 token 化的价值）。

---

## 二、字号体系与可读性

| # | 严重度 | 问题 | 位置 | 建议 |
|---|---|---|---|---|
| T1 | 🔴 高 | **字号碎片化**：verb 页约 25+ 个不同 font-size（0.55–1.9rem），无字阶约束 | 全站 | 收敛为 5 级字阶：xs .72 / sm .8 / base 1.0 / md 1.2 / lg 1.5rem，其余归并 |
| T2 | 🟠 中 | 极小字多处：ruby rt `.58rem`、score-bubble span `.62rem`、flip-hint `.69rem`、conjugation-tab small `.63rem` | 卡片/计分球/翻卡提示 | 辅助信息下限提到 `.72rem`（≈11.5px）；ruby rt 可保持例外但 ≥.65rem |
| T3 | 🟡 低 | `.assistive-copy`（.72rem）承载"统计保存在本机浏览器"等提示，偏次要 | 练习工具条 | 提高对比度（见 C1）而非仅调字号 |

---

## 三、交互反馈与状态

| # | 严重度 | 问题 | 位置 | 建议 |
|---|---|---|---|---|
| F1 | 🔴 高 | **键盘焦点几乎不可见**：primary/secondary/type-chip/mode-button/theme-option/count-select 均无 `:focus-visible`（verb/adj 页各仅 2 处） | 全部按钮 | 全局统一 `:focus-visible { outline: 3px solid var(--vermilion); outline-offset: 3px; }`，与现有链接焦点风格对齐 |
| F2 | 🟠 中 | `secondary-button` 无通用 `:disabled` 样式（仅 start-actions 特例）；radio/select 焦点依赖浏览器默认 | 按钮/表单 | 补 `.secondary-button:disabled` + select/radio 的 focus 样式 |
| F3 | 🟡 低 | 主题面板只能点按钮开合，**无 ESC / 外点关闭**；键盘进入菜单后缺少方向键支持 | css/themes.css | 补 ESC 关闭 + `role="menu"` 的方向键导航（或降级为简单 dialog 语义） |
| F4 | 🟡 低 | 卡片翻转有延迟级联动画（--card-delay），但无 hover 微反馈 | 规则卡片 | 可选：正面加"悬停微抬升"；不必强求 |

---

## 四、布局、对齐与间距

| # | 严重度 | 问题 | 位置 | 建议 |
|---|---|---|---|---|
| L1 | 🟠 中 | **间距缺乏统一网格**：padding 出现 13/14/15/16/18/22px 等 12 种组合；config 行 gap 10px 与面板 gap 14px 并存 | 全站容器 | 引入 8px 基准：4/8/12/16/24 档；面板 padding 统一 16px，卡片 20px |
| L2 | 🟠 中 | 首页 `page-heading` 三元素（标题/印章/主题按钮）space-between，**≤640px 时拥挤**（主题面板可能溢出或换行错位） | index.html | 移动端将主题按钮移入独立行或右对齐缩小 |
| L3 | 🟡 低 | 规则表 25 行高密度，正文与附件表之间仅 4px 间隔 | verb 规则页 | 附件区顶部留 12px，加"分区呼吸" |
| L4 | 🟡 低 | 练习页 config（类型/题数）+ 会话 + 题目 + 结果同屏堆叠 | verb/adj 练习页 | 会话进行中可折叠 config 为一行摘要 |

---

## 五、响应式适配

| # | 严重度 | 问题 | 位置 | 建议 |
|---|---|---|---|---|
| R1 | 🟠 中 | 断点仅 900/640 两级，**641–899px 平板横屏**表现未定义（规则表 860px min-width 在 700px 视口出现横向滚动属预期，但练习 config 未专门适配） | verb/adj | 补 768px 中间断点：config 行换行策略、结果区单列 |
| R2 | 🟠 中 | **触控目标偏小**：type-chip/mode-button/theme-option 高度约 28–32px，低于移动端 44px 建议 | 全站交互元素 | 移动端断点内将 chip/按钮 padding 加大至 ≥40px 高度 |
| R3 | 🟡 低 | theme-panel `min-width: 236px` + `right: 0`，320px 视口下贴近右缘，视觉拥挤 | css/themes.css | 移动端面板改为 `left: 0; right: auto` 或全宽下拉 |

---

## 六、信息密度与导航

| # | 严重度 | 问题 | 位置 | 建议 |
|---|---|---|---|---|
| N1 | 🟠 中 | 派生形 19 行整表无锚点/分组折叠，长页定位成本高 | verb 规则页（派生 tab） | 表顶加"行内锚点胶囊"（て形/た形/可能/被动…）或按子类分组小标题 |
| N2 | 🟡 低 | 练习一屏信息层级多（设置→题干→题目→操作→结果） | 练习页 | 明确三区块视觉边界：设置区用虚线框（已有）、题目区、结果区（已有）——可加分隔线强化 |
| N3 | 🟡 低 | 首页四个模块卡 + 学习提示，信息量仍可扫描，层级简单清晰 | index | 保持两列桌面、单列移动布局；新增模块继续检查卡片 min-height 对齐 |

---

## 七、修复优先级路线图

| 阶段 | 内容 | 影响 |
|---|---|---|
| **P0 无障碍底线**（建议先做） | C1–C5 对比度修复（themes.css 调色）+ F1 全局 focus-visible | 消除合规与键盘可达性问题，改动集中在 token 层，风险最低 |
| **P1 可读性** | T1 字阶收敛 + T2 小字下限 | 阅读体验整体提升，改动量中等 |
| **P2 细节打磨** | L1 间距网格、R2 触控目标、R1 中间断点、F3 ESC/菜单 | 质感与移动端体验 |
| **P3 结构性增强** | N1 锚点导航、L4 练习折叠、R3 面板移动适配 | 信息架构与长页体验 |

---

*附：对比度实测脚本与完整数据见会话记录；本报告对应代码版本 commit `07212d9`。*
